# Daily Plan: March 23, 2026

## Theme
GitHub account, public repository, and a minimal FastAPI backend

## Theory
- [x] Review how `GitHub` account, repository, `clone`, `commit`, and `push` are connected
- [x] Understand what a Python virtual environment `venv` is and why it is used
- [x] Review how a minimal HTTP API works with `GET` endpoints
- [x] Understand what `FastAPI` and `uvicorn` do in a Python backend project

## Practice
- [x] Register a personal account on `GitHub`
- [x] Create a public GitHub repository
- [x] Create a minimal API backend with `Python` and `FastAPI`
- [x] Add endpoint `GET /healthcheck` that returns a successful health status
- [x] Add endpoint `GET /` that returns `hello, world`
- [x] Add a `README.md` with installation steps, including creating `venv`
- [x] Add project start instructions to `README.md`
- [x] Add test `curl` requests to `README.md` for both endpoints

## Expected Result
- The student has a personal GitHub account and a public repository
- The repository contains a minimal working FastAPI backend
- The project can be installed in a virtual environment and started locally
- API endpoints can be checked with `curl` using examples from `README.md`

## AI Tasks
- [x] Ask AI to explain how `venv` works and why it is useful
- [ ] Ask AI to generate a minimal `FastAPI` application with two `GET` endpoints
- [ ] Ask AI to explain how to run `uvicorn`
- [ ] Ask AI to review the final `README.md` for clarity and completeness

## Report
In the daily report, include:
- which GitHub account and repository were created
- what files were added for the FastAPI backend
- how the backend is started locally
- what each endpoint returns
- whether the `curl` test requests worked successfully

### Report Criteria
- [ ] The report contains a short summary of the day
- [ ] The report mentions the created public repository `hello-fastapi-api`
- [ ] The report mentions that `venv` was created and used
- [ ] The report mentions endpoint `GET /healthcheck`
- [ ] The report mentions endpoint `GET /`
- [ ] The report includes the expected response `hello, world`
- [ ] The report confirms whether both `curl` requests worked
